name: respira_app
description: Aplikasi pemeriksaan kesehatan respirasi berbasis suara.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^0.68.0

  # Firebase
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  firebase_database: ^11.1.0

  # State Management
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
